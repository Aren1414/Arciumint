name: Deploy Anchor Program

on:
  push:
    branches: [main]

env:
  RUSTUP_TOOLCHAIN: nightly-2025-08-01

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Install Rust nightly
      uses: actions-rs/toolchain@v1
      with:
        toolchain: nightly-2025-08-01
        override: true
        components: rust-src

    - name: Verify Rust toolchain
      run: |
        rustup show
        rustc --version
        cargo --version

    - name: Update Cargo.lock (fix version 4 issue)
      run: |
        cd programs/arciumint-nft-gen
        rm -f Cargo.lock
        rustup run nightly-2025-08-01 cargo generate-lockfile -Znext-lockfile-bump
        cd ../..

    - name: Install Solana CLI v1.18.2
      run: |
        curl -sL https://github.com/solana-labs/solana/releases/download/v1.18.2/solana-release-x86_64-unknown-linux-gnu.tar.bz2 -o solana.tar.bz2
        tar -xjf solana.tar.bz2
        echo "$PWD/solana-release/bin" >> $GITHUB_PATH
        export PATH="$PWD/solana-release/bin:$PATH"
        solana --version

    - name: Install Anchor CLI
      run: |
        rustup run nightly-2025-08-01 cargo install --git https://github.com/coral-xyz/anchor anchor-cli --locked
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH
        export PATH="$HOME/.cargo/bin:$PATH"
        anchor --version

    - name: Restore wallet keypair
      run: |
        echo "${{ secrets.WALLET_KEY }}" > wallet.json
        solana config set --keypair wallet.json
        solana config set --url https://api.devnet.solana.com

    - name: Restore program keypair
      run: |
        mkdir -p programs/arciumint-nft-gen/target/deploy
        echo "${{ secrets.PROGRAM_KEYPAIR }}" > programs/arciumint-nft-gen/target/deploy/arciumintnftgen-keypair.json

    - name: Airdrop SOL to wallet
      run: solana airdrop 0.2 || echo "Airdrop failed, continuing anyway"

    - name: Sync Program ID automatically
      run: |
        cd programs/arciumint-nft-gen
        rustup run nightly-2025-08-01 anchor keys list
        cd ../..

    - name: Build Anchor program (nightly)
      run: |
        cd programs/arciumint-nft-gen
        rustup run nightly-2025-08-01 cargo build-bpf
        rustup run nightly-2025-08-01 cargo install --force --path .
        rustup run nightly-2025-08-01 anchor build --skip-lint
        cd ../..

    - name: Deploy program to devnet
      run: |
        cd programs/arciumint-nft-gen
        rustup run nightly-2025-08-01 anchor deploy --provider.cluster devnet --provider.wallet ../../wallet.json
