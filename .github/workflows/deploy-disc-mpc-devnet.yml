name: Deploy DISC MPC Program (Devnet)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      SOLANA_VERSION: "1.18.26"
      SOLANA_RPC_URL: https://api.devnet.solana.com

    steps:
      # --------------------------------------------------
      # 1) Checkout repo
      # --------------------------------------------------
      - name: Checkout
        uses: actions/checkout@v4

      # --------------------------------------------------
      # 2) Install Solana CLI (from GitHub release tarball)
      # --------------------------------------------------
      - name: Install Solana CLI
        shell: bash
        run: |
          set -e
          curl -L "https://github.com/solana-labs/solana/releases/download/v${SOLANA_VERSION}/solana-release-x86_64-unknown-linux-gnu.tar.bz2" -o solana.tar.bz2
          tar -xjf solana.tar.bz2
          sudo mv solana-release/bin/* /usr/local/bin/
          solana --version
          solana-keygen --version

      # --------------------------------------------------
      # 3) Write keypairs from GitHub Secrets (CORRECT NAMES)
      # --------------------------------------------------
      - name: Write Solana keypairs
        shell: bash
        run: |
          set -e

          mkdir -p ~/.config/solana
          mkdir -p arcium/disc_mpc/target/deploy

          # IMPORTANT:
          # Secrets that MUST exist in GitHub:
          # - SOLANA_WALLET_KEYPAIR
          # - DISC_MPC_PROGRAM_KEYPAIR

          printf '%s' '${{ secrets.SOLANA_WALLET_KEYPAIR }}' > ~/.config/solana/id.json
          printf '%s' '${{ secrets.DISC_MPC_PROGRAM_KEYPAIR }}' > arcium/disc_mpc/target/deploy/disc_mpc-keypair.json

          chmod 600 ~/.config/solana/id.json
          chmod 600 arcium/disc_mpc/target/deploy/disc_mpc-keypair.json

          solana config set --url "${SOLANA_RPC_URL}"
          solana config set --keypair ~/.config/solana/id.json
          solana config get

          # sanity check (no secret leakage)
          solana-keygen pubkey ~/.config/solana/id.json
          solana-keygen pubkey arcium/disc_mpc/target/deploy/disc_mpc-keypair.json

      # --------------------------------------------------
      # 4) Airdrop SOL (best effort – devnet rate limits)
      # --------------------------------------------------
      - name: Airdrop SOL
        shell: bash
        run: |
          set -e
          solana airdrop 2 || solana airdrop 1 || true
          solana balance || true

      # --------------------------------------------------
      # 5) Install Rust toolchain (MATCHES rust-toolchain.toml)
      # --------------------------------------------------
      - name: Install Rust 1.89.0
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.89.0"
          components: rustfmt, clippy

      # --------------------------------------------------
      # 6) Install Node + Yarn
      # --------------------------------------------------
      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Enable Corepack (Yarn)
        shell: bash
        run: |
          corepack enable
          yarn --version

      # --------------------------------------------------
      # 7) Install Anchor CLI (compatible with Solana 1.18)
      # --------------------------------------------------
      - name: Install Anchor CLI
        shell: bash
        run: |
          set -e
          cargo install --git https://github.com/coral-xyz/anchor --tag v0.32.1 anchor-cli
          anchor --version

      # --------------------------------------------------
      # 8) Build & Deploy DISC MPC program
      # --------------------------------------------------
      - name: Build & Deploy (Anchor)
        shell: bash
        working-directory: arcium/disc_mpc
        run: |
          set -e
          yarn install --frozen-lockfile || yarn install
          anchor build
          anchor deploy --provider.cluster devnet

      # --------------------------------------------------
      # 9) OPTIONAL – init computation definition (run once)
      # --------------------------------------------------
      - name: Init computation definition (optional)
        shell: bash
        working-directory: arcium/disc_mpc
        run: |
          set -e
          # If you want to init on devnet once, uncomment:
          # yarn ts-node scripts/init-devnet.ts
          echo "Init step skipped (optional)"
